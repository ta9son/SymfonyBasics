<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>{{title}}</title>
    </head>
    <body>
        <h1>{{title}}</h1>
        <p>{{msg | raw}}</p>
        {{form_start(form)}}
        {{form_widget(form)}}
        {{form_end(form)}}
    </body>
</html>