<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>{{title}}</title>
    </head>
    <body>
        <h1>{{title}}</h1>
        <p>{{msg}}</p>
        <form action="/hello" method="post">
            <input type="text" name="input">
            <input type="submit" value="submit">
    </body>
</html>